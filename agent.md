# VideoTracker 提交历史与开发记录

本文档用于记录开发过程中的 Git Commit 历史与各个阶段的功能点实现。

## 提交历史日志 (Git Commit Logs)

*   **Initial Commit** (ff2c512)
    *   **时间**: 2026-06-21
    *   **描述**: 初始化项目目录，创建项目 Git 仓库与 `agent.md` 记录文件。

*   **feat: 项目开发骨架与基础脚手架搭建完成** (8ac1678)
    *   **时间**: 2026-06-21
    *   **描述**: 完成 Electron + React + TypeScript + Vite + Tailwind CSS 脚手架搭建，编写主进程入口、Preload 预加载 API 以及开发/构建自动化脚本。

*   **feat: 实现核心组件与数据服务开发** (9159cd0)
    *   **时间**: 2026-06-21
    *   **描述**: 编写完成了 ArtPlayer 播放器封装、多源目录树异步解析、自动计时（含鼠标/键盘防挂机检测）、WebDAV 双向数据同步合并算法、学习进度仪表盘和全量数据统计大屏等全套核心业务组件。

*   **fix: 修复 esbuild 与 Vite 编译配置，完美适配 Tailwind CSS v4** (b76b043)
    *   **时间**: 2026-06-21
    *   **描述**: 修正了 esbuild 编译 flags，指定 Vite root 指向渲染进程目录，调整 CSS 的 Tailwind 导入语法，配置 postcss.config 引入新版 @tailwindcss/postcss。

*   **fix: 强绑定 127.0.0.1 保证主进程成功加载页面** (e329f80)
    *   **时间**: 2026-06-21
    *   **描述**: 锁定 Vite 监听 IP 为 127.0.0.1，优化 Electron 主进程窗口的 did-fail-load 重试，解决并发加载引起的白屏。

*   **chore: 添加一键快速启动批处理脚本 run.bat** (a8c48eb)
    *   **时间**: 2026-06-21
    *   **描述**: 在项目根目录下创建了 `run.bat`，设定好国内 Electron 二进制下载镜像环境，简化用户在 Windows 下的双击启动操作。

*   **fix: 修复 run.bat 调用指令，使用 call 防止执行权流失闪退** (661661c)
    *   **时间**: 2026-06-21
    *   **描述**: 修改了 `run.bat`，将 `npm run dev` 替换为 `call npm run dev`，确保在错误退出时窗口能停留在 `pause` 处呈现真实报错。

*   **fix: 转换 run.bat 为纯英文，彻底避免中文编码乱码与分行解析错误** (1bf0d2a)
    *   **时间**: 2026-06-21
    *   **描述**: 移除批处理脚本内所有中文注释与提示，采用纯英文 ASCII 格式，根治了 Windows CMD 读取 UTF-8 文件时导致的乱码及解析崩坏。

*   **fix: 修复选项高亮融色问题与本地中文路径播放解析错误** (8661a63)
    *   **时间**: 2026-06-21
    *   **描述**: 在 index.css 声明 Tailwind v4 @theme，使 bg-primary 生效解决高亮白屏；对 base64 路径做 URL 编码及空格转加号容错，修复中文大纲视频无法播放的故障。

*   **feat: 优化双侧边栏拉伸折叠、底部响应式自适应、全局课时统计、视频大小/时长展示及上次播放进度条背景填充** (7ed904e)
    *   **时间**: 2026-06-21
    *   **描述**: 1. 重构主界面为自适应 Flex 双侧栏拉伸折叠架构，配合浮动气泡按钮实现一键对称折叠；2. 状态提升文件树，让 Dashboard 真正统计全局视频而非仅已播记录；3. 视频项下方展示大小与时长，且以淡主色背景横向填充上次播放百分比进度；4. 重构 Dashboard 布局为流式 flex-wrap，防止收窄遮挡与重合。

*   **fix: 修复 Dashboard 播放倍速下拉菜单无法选中 2.0x 及以上倍速的 React 状态匹配 Bug** (f286833)
    *   **时间**: 2026-06-21
    *   **描述**: 修改 option 选项 value，将 float strings (2.0, 3.0) 调整为数字标准 String(number) 对等的整数形式 (2, 3)，解决 React 状态匹配不一致导致回退 1 倍的 Bug。

*   **feat: 优化左右拖拽手柄及折叠按钮为 hover 悬浮渐显设计，重构右栏为统一毛玻璃背板避免内容遮挡** (841ca3a)
    *   **时间**: 2026-06-21
    *   **描述**: 1. 手柄及折叠气泡按钮重构为 hover 悬浮渐显交互，非悬停时不显示突兀线条及按钮；2. 折叠时按钮收缩为边缘精致小拉手；3. 给右侧日历加上统一的毛玻璃背板与 padding 间距，彻底消除手柄对日历内容的遮挡，微调卡片样式为清透叠色。

*   **feat: 重构右侧日历手柄及背板为最外层三栏并行 Flex 架构，消除内边距定位偏差，实现 100% 物理边缘无缝重合** (942b762)
    *   **时间**: 2026-06-21
    *   **描述**: 将右栏和右手柄从 main 内抽离，作为与左栏、main 并列的最外层平行子节点。这彻底排除了 padding 和 gap 导致的手柄对位偏移 Bug，使手柄及气泡按钮 100% 精准对齐右侧毛玻璃边界。

*   **fix: 修复视频选项卡片中播放图标未在高度上垂直居中的对齐 Bug** (9941d48)
    *   **时间**: 2026-06-21
    *   **描述**: 将选项卡片重构为左右并排的 Flex items-center 结构，左侧放播放图标，右侧放包含标题与元数据双行纵向 flex 容器，实现图标与整个双行高度完美垂直居中，并消除多余的 pl-6.5 硬编码间距。
*   **fix: 修复视频选项播放图标的容器居中，解决视觉偏上不居中的Bug** (ee05ef0)
    *   **时间**: 2026-06-21
    *   **描述**: 将 `renderProgressIcon` 里的播放与完成状态 `span` 图标包裹于 `w-6 h-6 flex items-center justify-center` 容器中，并添加 `leading-none` 消除字体渲染的默认行高 baseline 偏移，从而确保多行文本卡片下，图标在视觉上实现绝对的水平和垂直居中。

*   **feat: 实现了视频已学完点/标签、右键状态切换、静默时长扫描、CustomSelect 及统计点击切换** (eeb390a)
    *   **时间**: 2026-06-21
    *   **描述**: 1. 新增已学完成状态判定，在播放器播放完成或手动标记后，左侧视频列表在图标旁展示精致小绿点及淡绿“已学完”徽章；2. 添加卡片右键 contextMenu 自定义浮层，支持一键切换学完/未学完并同步计算 100%/0% 进度；3. 新增后台低优先级顺序元数据扫描器，通过隐藏 video 异步读取未播视频的时长写入本地存储；4. 封装毛玻璃 CustomSelect 组件，全局无缝替换所有丑陋的原生 select 控件；5. 底部统计栏支持点击在“课时数/累计时长”中切换并更新 Dashboard 相关估算。

*   **fix: 将 Dashboard 计划面板与播放器容器位置对调，解决最底部下拉菜单被遮挡/剪切的视觉 Bug** (2589b52)
    *   **时间**: 2026-06-21
    *   **描述**: 将 React 布局中的 `Dashboard` 面板移动到播放器容器 `div` 上方。这在物理上避免了倍速下拉框向下弹出时被屏幕底部边缘截断，保证了全部倍速选项（1.0x - 3.0x）的完整可见。

*   **fix: 显式设置 Dashboard 及其内层容器的 overflow-visible 和 z-index，彻底解决下拉菜单被剪裁/遮挡 of 视觉 Bug** (1ac09d2)
    *   **时间**: 2026-06-21
    *   **描述**: 为 `Dashboard` 顶级圆角卡片及内部的微灰胶囊容器添加 `overflow-visible` 并设定 `relative z-20` 层叠层，解决了由于 `backdrop-filter` 形成包含块及圆角层导致绝对定位下拉面板被截断裁剪的底层渲染 Bug，使其能够完美、无缝地置顶并覆盖展示在下方播放器之上。

*   **feat: 播放器暂停美化、全局倍速状态提升与快捷键绑定、设置页录制及重置、下拉框遮挡优化** (ff5714b)
    *   **时间**: 2026-06-21
    *   **描述**: 1. index.css 覆盖大播放图标，增加蓝色发光投影以提升在白底PPT中的辨识度；2. 倍速状态提升至 App，共享至 Player 与 Dashboard 仪表盘并动态向下拉框注入当前倍速，实现快捷键（F全屏、C加速、X减速、Z重置倍速）后的双向强联动；3. Settings 录制修改为全局捕获事件并拦截所有系统按键行为，优化录制 UI 态和 Esc 取消逻辑，一键重置全量参数；4. CustomSelect 支持 fullWidth，左侧挂载源下拉菜单截断防止溢出裁剪，重整列表与下拉框 z-index 层叠避免遮挡。

*   **feat: 支持自定义置顶悬浮播放器小窗口，解决非激活态挂机不计时，支持取消置顶与退出唤醒聚焦** (c81b14b)
    *   **时间**: 2026-06-21
    *   **描述**: 1. preload.ts 暴露置顶与小窗控制方法；2. index.ts 注册 pip 独立无边框 BrowserWindow 并通过 query 串无感传参，监听 window.closed 自动 mainWindow.show/focus 唤醒聚焦；3. 编写 PipPlayer.tsx 独立页面在置顶小窗下播放，将 idleTimeoutMinutes 设大以实际上免除挂机检测，支持拖拽、开启/取消置顶按钮与进度存储；4. App.tsx 根据 ?mode=pip 分流加载浮窗，Player.tsx 自定义 PiP 控制图标，并在 window.closed 时自动同步最新播放时间点。

*   **feat: 回归原生画中画播放，优化原生PiP下自动累加计时 and 退出画中画时唤醒聚焦主程序** (f740a77)
    *   **时间**: 2026-06-21
    *   **描述**: 1. 回滚主进程 pip 独立子窗口的所有代码，并注册通用的 window:focus 唤醒聚焦 IPC 通道；2. preload.ts 只暴露 focusMainWindow 方法，清理 App.tsx 路由分流与 PipPlayer.tsx 物理文件；3. 开启 ArtPlayer 原生画中画 (pip: true)，监听 video:leavepictureinpicture 原生事件，在画中画退出时调用 focusMainWindow 自动聚焦唤起主程序；4. useTimer.ts 定时器中检测到 document.pictureInPictureElement 时自动刷新活跃时间，免除闲置防挂机超时，保证原生画中画下正常记录学习时长。

*   **feat: 禁用主窗口后台限频，确保原生画中画播放时计时器正常顺畅走字** (1022099)
    *   **时间**: 2026-06-21
    *   **描述**: 在主窗口的 webPreferences 中显式配置 `backgroundThrottling: false`，彻底消除主台失焦并置于后台时 Chromium 对定时器的 CPU 挂起降频机制，保证原生画中画播放中主界面“本日计时”气泡数值正常且无任何卡顿。
